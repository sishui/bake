// Code generated by bake. DO NOT EDIT.
// version: {{.Version}}
// source: {{.Table}}

// Package {{.Package}} defines database models generated from schema.
package {{.Package}}

{{- if gt (len .Imports) 0}}{{ "\n" }}
import (
{{- $lastIndex := sub (len .Imports) 1 }}
{{- range $i, $group := .Imports }}
	{{- range $group }}
		{{- "\n\t" }}"{{ . }}"
	{{- end }}
	{{- if ne $i $lastIndex }}{{ "\n" }}{{ end }}
{{- end }}
)
{{- end}}

var Nil{{.Model}} = (*{{.Model}})(nil)

var (
	_ bun.BeforeInsertHook = (*{{.Model}})(nil)
	_ bun.BeforeUpdateHook = (*{{.Model}})(nil)
)

const (
	{{.Model}}TableName  = "{{.Table}}"
	{{.Model}}TableAlias = "{{.Alias}}"
)

var (
	{{.Model}}TableNameIdent  = bun.Ident("{{.Table}}")
	{{.Model}}TableAliasIdent = bun.Ident("{{.Alias}}")
)

const (
	InnerJoin{{.Model}}AsAlias = "INNER JOIN {{.Table}} AS {{.Alias}}" // same as JOIN
	LeftJoin{{.Model}}AsAlias  = "LEFT JOIN {{.Table}} AS {{.Alias}}"  // same as LEFT OUTER JOIN
	RightJoin{{.Model}}AsAlias = "RIGHT JOIN {{.Table}} AS {{.Alias}}" // same as RIGHT OUTER JOIN
	FullJoin{{.Model}}AsAlias  = "FULL JOIN {{.Table}} AS {{.Alias}}"  // same as FULL OUTER JOIN
)

{{ $model := .Model -}}
{{ $maxFieldLength := .MaxFieldLength -}}
{{ $maxNullableLength := .MaxNullableLength -}}
{{ $maxRelationLength := .MaxRelationLength -}}
{{ $maxOrderedLength := .MaxOrderedLength -}}
{{ $maxOrderedNonStringLength := .MaxOrderedNonStringLength -}}
{{ $maxStringLength := .MaxStringLength -}}
{{ $maxNumericLength := .MaxNumericLength -}}
{{ $maxEquatableLength := .MaxEquatableLength -}}
{{ $maxArithmeticLength := .MaxArithmeticLength -}}
{{ $maxTimeLength := .MaxTimeLength -}}
{{ $alias := .Alias -}}
{{ $createdAtType := .CreatedAtType -}}
{{ $updatedAtType := .UpdatedAtType -}}
{{ $deletedAtType := .DeletedAtType -}}
{{ $timezone    := .Timezone -}}

const (
{{- range $field := .Fields}}
	{{- if not $field.IsRelation }}
	{{- "\n\t" }}{{$model}}{{ align $field.Name $maxFieldLength }} = "{{$field.ColumnName}}"
	{{- end }}
{{- end}}
)

{{- if gt $maxRelationLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if $field.IsRelation }}
	{{- "\n\t" }}{{$model}}Relation{{ align $field.Name $maxRelationLength }} = "{{$field.Name}}"
	{{- end}}
{{- end}}
)
{{- end}}

const (
{{- range $field := .Fields}}
	{{- if not $field.IsRelation }}
	{{- "\n\t" }}{{$model}}Alias{{ align $field.Name $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}}"
	{{- end }}
{{- end}}
)

var (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Ident" $maxFieldLength }} = bun.Ident("{{$field.ColumnName}}")
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Eq" $maxFieldLength }} = "{{$field.ColumnName}} = ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Eq" $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}} = ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Neq" $maxFieldLength }} = "{{$field.ColumnName}} <> ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Neq" $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}} <> ?"
	{{- end}}
{{- end}}
)

{{- if gt $maxOrderedNonStringLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Gt" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} > ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Gt" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} > ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Gte" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} >= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Gte" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} >= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Lt" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} < ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Lt" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} < ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Lte" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} <= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Lte" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} <= ?"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxStringLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Like" $maxStringLength }} = "{{$field.ColumnName}} LIKE ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Like" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikePrefix" $maxStringLength }} = "{{$field.ColumnName}} LIKE ?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikePrefix" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE ?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikeSuffix" $maxStringLength }} = "{{$field.ColumnName}} LIKE %?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikeSuffix" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE %?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikeContain" $maxStringLength }} = "{{$field.ColumnName}} LIKE %?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikeContain" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE %?%"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxArithmeticLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SUMExpr" $maxArithmeticLength }} = "SUM({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SUMExpr" $maxArithmeticLength }} = "SUM({{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "AVGExpr" $maxArithmeticLength }} = "AVG({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "AVGExpr" $maxArithmeticLength }} = "AVG({{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxOrderedLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "MINExpr" $maxOrderedLength }} = "MIN({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "MINExpr" $maxOrderedLength }} = "MIN({{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "MAXExpr" $maxOrderedLength }} = "MAX({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "MAXExpr" $maxOrderedLength }} = "MAX({{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxArithmeticLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "AddExpr" $maxArithmeticLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} + ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "AddExpr" $maxArithmeticLength }} = "{{$alias}}.{{$field.ColumnName}} = {{$alias}}.{{$field.ColumnName}} + ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SubExpr" $maxArithmeticLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} - ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SubExpr" $maxArithmeticLength }} = "{{$alias}}.{{$field.ColumnName}} = {{$alias}}.{{$field.ColumnName}} - ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "AddLeastExpr" $maxArithmeticLength }} = "{{$field.ColumnName}} = LEAST({{$field.ColumnName}} + ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "AddLeastExpr" $maxArithmeticLength }} = "{{$alias}}.{{$field.ColumnName}} = LEAST({{$alias}}.{{$field.ColumnName}} + ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SubGreatestExpr" $maxArithmeticLength }} = "{{$field.ColumnName}} = GREATEST({{$field.ColumnName}} - ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SubGreatestExpr" $maxArithmeticLength }} = "{{$alias}}.{{$field.ColumnName}} = GREATEST({{$alias}}.{{$field.ColumnName}} - ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "ClampExpr" $maxArithmeticLength }} = "{{$field.ColumnName}} = LEAST(GREATEST({{$field.ColumnName}} + ?, ?), ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if and (eq $field.Kind "NUMERIC") (not $field.IsPrimary) }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "ClampExpr" $maxArithmeticLength }} = "{{$alias}}.{{$field.ColumnName}} = LEAST(GREATEST({{$alias}}.{{$field.ColumnName}} + ?, ?), ?)"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxEquatableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "In" $maxEquatableLength }} = "{{$field.ColumnName}} IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "In" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "NotIn" $maxEquatableLength }} = "{{$field.ColumnName}} NOT IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "NotIn" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} NOT IN (?)"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxNullableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "IsNull" $maxNullableLength }} = "{{$field.ColumnName}} IS NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "IsNull" $maxNullableLength }} = "{{$alias}}.{{$field.ColumnName}} IS NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "IsNotNull" $maxNullableLength }} = "{{$field.ColumnName}} IS NOT NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "IsNotNull" $maxNullableLength }} = "{{$alias}}.{{$field.ColumnName}} IS NOT NULL"
	{{- end}}
{{- end}}
)

// COALESCE expressions (COALESCE(column, ?))
const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "CoalesceExpr" $maxNullableLength }} = "COALESCE({{$field.ColumnName}}, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "CoalesceExpr" $maxNullableLength }} = "COALESCE({{$alias}}.{{$field.ColumnName}}, ?)"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxOrderedNonStringLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Between" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Between" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "NotBetween" $maxOrderedNonStringLength }} = "{{$field.ColumnName}} NOT BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "NotBetween" $maxOrderedNonStringLength }} = "{{$alias}}.{{$field.ColumnName}} NOT BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxTimeLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "DateExpr" $maxTimeLength }} = "DATE({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "DateExpr" $maxTimeLength }} = "DATE({{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "YearExpr" $maxTimeLength }} = "EXTRACT(YEAR FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "YearExpr" $maxTimeLength }} = "EXTRACT(YEAR FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "MonthExpr" $maxTimeLength }} = "EXTRACT(MONTH FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "MonthExpr" $maxTimeLength }} = "EXTRACT(MONTH FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "DayExpr" $maxTimeLength }} = "EXTRACT(DAY FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "DayExpr" $maxTimeLength }} = "EXTRACT(DAY FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "HourExpr" $maxTimeLength }} = "EXTRACT(HOUR FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "HourExpr" $maxTimeLength }} = "EXTRACT(HOUR FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "MinuteExpr" $maxTimeLength }} = "EXTRACT(MINUTE FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "MinuteExpr" $maxTimeLength }} = "EXTRACT(MINUTE FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SecondExpr" $maxTimeLength }} = "EXTRACT(SECOND FROM {{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "TIME" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SecondExpr" $maxTimeLength }} = "EXTRACT(SECOND FROM {{$alias}}.{{$field.ColumnName}})"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxEquatableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Asc" $maxEquatableLength }} = "{{$field.ColumnName}} ASC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Asc" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} ASC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Desc" $maxEquatableLength }} = "{{$field.ColumnName}} DESC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Desc" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} DESC"
	{{- end}}
{{- end}}
)
{{- end}}

// {{.Model}} generates a model for the "{{.Table}}" table.
{{- range $comment :=.Comments}}
// {{$comment -}}
{{- end}}
type {{.Model}} struct {
	{{ .BunModel }} `bun:"table:{{.Table}},alias:{{.Alias}}"`
	{{- "\n"}}
	{{- range $field := .Fields}}
	{{- if gt (len $field.Comments) 1}}
	{{- range $comment := $field.Comments}}
	// {{$comment}}
	{{- end}}
	{{$field.AlignedName}} {{$field.AlignedType}} {{$field.AlignedTag}}
	{{- else}}
	{{$field.AlignedName}} {{$field.AlignedType}} {{$field.AlignedTag}} //{{ if gt (len .Comments) 0 -}} {{ " " }}{{ index .Comments 0 -}}{{ end }}
	{{- end}}
	{{- end}}
}

func (m *{{.Model}}) BeforeInsert(ctx context.Context, query *bun.InsertQuery) error {
{{- if or $createdAtType $updatedAtType }}
{{- if eq $timezone "UTC" }}
	now := time.Now().UTC()
{{- else if eq $timezone "Local" }}
	now := time.Now().Local()
{{- else if $timezone }}
	loc, err := time.LoadLocation("{{$timezone}}")
	if err != nil {
		return err
	}
	now := time.Now().In(loc)
{{- else }}
	now := time.Now()
{{- end }}

{{- if $createdAtType }}
{{- if eq $createdAtType "time.Time" }}
	if m.CreatedAt.IsZero() {
		m.CreatedAt = now
	}
{{- else if eq $createdAtType "*time.Time" }}
	if m.CreatedAt == nil {
		m.CreatedAt = &now
	}
{{- else if eq $createdAtType "int64" }}
	if m.CreatedAt == 0 {
		m.CreatedAt = now.UnixMilli()
	}
{{- else if eq $createdAtType "*int64" }}
	if m.CreatedAt == nil {
		nowMs := now.UnixMilli()
		m.CreatedAt = &nowMs
	}
{{- else if eq $createdAtType "int32" }}
	if m.CreatedAt == 0 {
		m.CreatedAt = int32(now.Unix())
	}
{{- else if eq $createdAtType "*int32" }}
	if m.CreatedAt == nil {
		nowSec := int32(now.Unix())
		m.CreatedAt = &nowSec
	}
{{- end }}
{{- end }}

{{- if $updatedAtType }}
{{- if eq $updatedAtType "time.Time" }}
	if m.UpdatedAt.IsZero() {
		m.UpdatedAt = now
	}
{{- else if eq $updatedAtType "*time.Time" }}
	if m.UpdatedAt == nil {
		m.UpdatedAt = &now
	}
{{- else if eq $updatedAtType "int64" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = now.UnixMilli()
	}
{{- else if eq $updatedAtType "*int64" }}
	if m.UpdatedAt == nil {
		nowMs := now.UnixMilli()
		m.UpdatedAt = &nowMs
	}
{{- else if eq $updatedAtType "int32" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = int32(now.Unix())
	}
{{- else if eq $updatedAtType "*int32" }}
	if m.UpdatedAt == nil {
		nowSec := int32(now.Unix())
		m.UpdatedAt = &nowSec
	}
{{- end }}
{{- end }}
{{- end }}
	return nil
}

func (m *{{.Model}}) BeforeUpdate(ctx context.Context, query *bun.UpdateQuery) error {
{{- if $updatedAtType }}
{{- if eq $timezone "UTC" }}
	now := time.Now().UTC()
{{- else if eq $timezone "Local" }}
	now := time.Now().Local()
{{- else if $timezone }}
	loc, err := time.LoadLocation("{{$timezone}}")
	if err != nil {
		return err
	}
	now := time.Now().In(loc)
{{- else }}
	now := time.Now()
{{- end }}

{{- if eq $updatedAtType "time.Time" }}
	if m.UpdatedAt.IsZero() {
		m.UpdatedAt = now
	}
{{- else if eq $updatedAtType "*time.Time" }}
	if m.UpdatedAt == nil {
		m.UpdatedAt = &now
	}
{{- else if eq $updatedAtType "int64" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = now.UnixMilli()
	}
{{- else if eq $updatedAtType "*int64" }}
	if m.UpdatedAt == nil {
		nowMs := now.UnixMilli()
		m.UpdatedAt = &nowMs
	}
{{- else if eq $updatedAtType "int32" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = int32(now.Unix())
	}
{{- else if eq $updatedAtType "*int32" }}
	if m.UpdatedAt == nil {
		nowSec := int32(now.Unix())
		m.UpdatedAt = &nowSec
	}
{{- end }}
{{- end }}
	return nil
}
