// Code generated by bake. DO NOT EDIT.
// version: {{.Version}}
// source: {{.Table}}

// Package {{.Package}} defines database models generated from schema.
package {{.Package}}

{{- if gt (len .Imports) 0}}{{ "\n" }}
import (
{{- $lastIndex := sub (len .Imports) 1 }}
{{- range $i, $group := .Imports }}
	{{- range $group }}
		{{- "\n\t" }}"{{ . }}"
	{{- end }}
	{{- if ne $i $lastIndex }}{{ "\n" }}{{ end }}
{{- end }}
)
{{- end}}

var Nil{{.Model}} = (*{{.Model}})(nil)

var (
	_ bun.BeforeInsertHook = (*{{.Model}})(nil)
	_ bun.BeforeUpdateHook = (*{{.Model}})(nil)
)

const (
	{{.Model}}TableName  = "{{.Table}}"
	{{.Model}}TableAlias = "{{.Alias}}"
)

var (
	{{.Model}}TableNameIdent  = bun.Ident("{{.Table}}")
	{{.Model}}TableAliasIdent = bun.Ident("{{.Alias}}")
)

const (
	InnerJoin{{.Model}}AsAlias = "INNER JOIN {{.Table}} AS {{.Alias}}" // same as JOIN
	LeftJoin{{.Model}}AsAlias  = "LEFT JOIN {{.Table}} AS {{.Alias}}"  // same as LEFT OUTER JOIN
	RightJoin{{.Model}}AsAlias = "RIGHT JOIN {{.Table}} AS {{.Alias}}" // same as RIGHT OUTER JOIN
	FullJoin{{.Model}}AsAlias  = "FULL JOIN {{.Table}} AS {{.Alias}}"  // same as FULL OUTER JOIN
)

{{ $model := .Model -}}
{{ $maxFieldLength := .MaxFieldLength -}}
{{ $maxNullableLength := .MaxNullableLength -}}
{{ $maxRelationLength := .MaxRelationLength -}}
{{ $maxOrderedLength := .MaxOrderedLength -}}
{{ $maxStringLength := .MaxStringLength -}}
{{ $maxNumericLength := .MaxNumericLength -}}
{{ $maxEquatableLength := .MaxEquatableLength -}}
{{ $alias := .Alias -}}
{{ $createdAtType := .CreatedAtType -}}
{{ $updatedAtType := .UpdatedAtType -}}
{{ $deletedAtType := .DeletedAtType -}}
{{ $timezone    := .Timezone -}}
{{ $isLineBreak := false -}}
const (
{{- range $field := .Fields}}
	{{- "\n\t" }}{{$model}}{{ align $field.Name $maxFieldLength }} = "{{$field.ColumnName}}"
{{- end}}
)

{{- if gt $maxRelationLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if $field.IsRelation }}
	{{- "\n\t" }}{{$model}}Relation{{ align $field.Name $maxRelationLength }} = "{{$field.Name}}"
	{{- end}}
{{- end}}
)
{{- end}}

const (
{{- range $field := .Fields}}
	{{- "\n\t" }}{{$model}}Alias{{ align $field.Name $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}}"
{{- end}}
)

var (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Ident" $maxFieldLength }} = bun.Ident("{{$field.ColumnName}}")
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Eq" $maxFieldLength }} = "{{$field.ColumnName}} = ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Eq" $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}} = ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Neq" $maxFieldLength }} = "{{$field.ColumnName}} <> ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if not $field.IsCustom }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Neq" $maxFieldLength }} = "{{$alias}}.{{$field.ColumnName}} <> ?"
	{{- end}}
{{- end}}
)

{{- if gt $maxOrderedLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Gt" $maxOrderedLength }} = "{{$field.ColumnName}} > ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Gt" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} > ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Gte" $maxOrderedLength }} = "{{$field.ColumnName}} >= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Gte" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} >= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Lt" $maxOrderedLength }} = "{{$field.ColumnName}} < ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Lt" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} < ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Lte" $maxOrderedLength }} = "{{$field.ColumnName}} <= ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Lte" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} <= ?"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxStringLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Like" $maxStringLength }} = "{{$field.ColumnName}} LIKE ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Like" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikePrefix" $maxStringLength }} = "{{$field.ColumnName}} LIKE ?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikePrefix" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE ?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikeSuffix" $maxStringLength }} = "{{$field.ColumnName}} LIKE %?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikeSuffix" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE ?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "LikeContain" $maxStringLength }} = "{{$field.ColumnName}} LIKE %?%"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "STRING" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "LikeContain" $maxStringLength }} = "{{$alias}}.{{$field.ColumnName}} LIKE %?%"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxNumericLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SUMExpr" $maxNumericLength }} = "SUM({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SUMExpr" $maxNumericLength }} = "SUM({{$field.ColumnName}})"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "AddExpr" $maxNumericLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} + ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "AddExpr" $maxNumericLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} + ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SubExpr" $maxNumericLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} - ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SubExpr" $maxNumericLength }} = "{{$field.ColumnName}} = {{$field.ColumnName}} - ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "AddLeastExpr" $maxNumericLength }} = "{{$field.ColumnName}} = LEAST({{$field.ColumnName}} + ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "AddLeastExpr" $maxNumericLength }} = "{{$field.ColumnName}} = LEAST({{$field.ColumnName}} + ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "SubGreatestExpr" $maxNumericLength }} = "{{$field.ColumnName}} = GREATEST({{$field.ColumnName}} - ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "SubGreatestExpr" $maxNumericLength }} = "{{$field.ColumnName}} = GREATEST({{$field.ColumnName}} - ?, ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "ClampExpr" $maxNumericLength }} = "{{$field.ColumnName}} = LEAST(GREATEST({{$field.ColumnName}} + ?, ?), ?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if eq $field.Kind "NUMERIC" }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "ClampExpr" $maxNumericLength }} = "{{$field.ColumnName}} = LEAST(GREATEST({{$field.ColumnName}} + ?, ?), ?)"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxEquatableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "In" $maxEquatableLength }} = "{{$field.ColumnName}} IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "In" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "NotIn" $maxEquatableLength }} = "{{$field.ColumnName}} NOT IN (?)"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "NotIn" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} NOT IN (?)"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxNullableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "IsNull" $maxNullableLength }} = "{{$field.ColumnName}} IS NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "IsNull" $maxNullableLength }} = "{{$alias}}.{{$field.ColumnName}} IS NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "IsNotNull" $maxNullableLength }} = "{{$field.ColumnName}} IS NOT NULL"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if $field.IsNullable }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "IsNotNull" $maxNullableLength }} = "{{$alias}}.{{$field.ColumnName}} IS NOT NULL"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxOrderedLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Between" $maxOrderedLength }} = "{{$field.ColumnName}} BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Between" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "NotBetween" $maxOrderedLength }} = "{{$field.ColumnName}} NOT BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "TIME") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "NotBetween" $maxOrderedLength }} = "{{$alias}}.{{$field.ColumnName}} NOT BETWEEN ? AND ?"
	{{- end}}
{{- end}}
)
{{- end}}

{{- if gt $maxEquatableLength 0 -}}
{{- "\n\n" -}}
const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Asc" $maxEquatableLength }} = "{{$field.ColumnName}} ASC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Asc" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} ASC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}{{ concat $field.Name "Desc" $maxEquatableLength }} = "{{$field.ColumnName}} DESC"
	{{- end}}
{{- end}}
)

const (
{{- range $field := .Fields}}
	{{- if or (eq $field.Kind "NUMERIC") (eq $field.Kind "STRING") (eq $field.Kind "BOOL") (eq $field.Kind "TIME") (eq $field.Kind "ENUM") }}
	{{- "\n\t" }}{{$model}}Alias{{ concat $field.Name "Desc" $maxEquatableLength }} = "{{$alias}}.{{$field.ColumnName}} DESC"
	{{- end}}
{{- end}}
)
{{- end}}

// {{.Model}} generates a model for the "{{.Table}}" table.
{{- range $comment :=.Comments}}
// {{$comment -}}
{{- end}}
type {{.Model}} struct {
	{{ .BunModel }} `bun:"table:{{.Table}},alias:{{.Alias}}"`
	{{- range $field := .Fields}}
	{{- if gt (len $field.Comments) 1}}
	{{- range $comment := $field.Comments}}
	// {{$comment}}
	{{- end}}
	{{$field.AlignedName}} {{$field.AlignedType}} {{$field.AlignedTag}}
	{{- else}}
	{{$field.AlignedName}} {{$field.AlignedType}} {{$field.AlignedTag}} //{{ if gt (len .Comments) 0 -}} {{ " " }}{{ index .Comments 0 -}}{{ end }}
	{{- end}}
	{{- end}}
}

func (m *{{.Model}}) BeforeInsert(ctx context.Context, query *bun.InsertQuery) error {
{{- if or $createdAtType $updatedAtType }}
{{- if eq $timezone "UTC" }}
	now := time.Now().UTC()
{{- else if eq $timezone "Local" }}
	now := time.Now().Local()
{{- else if $timezone }}
	loc, err := time.LoadLocation("{{$timezone}}")
	if err != nil {
		return err
	}
	now := time.Now().In(loc)
{{- else }}
	now := time.Now()
{{- end }}
{{- end }}

{{- if $createdAtType }}
{{- if eq $createdAtType "time.Time" }}
	if m.CreatedAt.IsZero() {
		m.CreatedAt = now
	}
{{- else if eq $createdAtType "*time.Time" }}
	if m.CreatedAt == nil {
		m.CreatedAt = &now
	}
{{- else if eq $createdAtType "int64" }}
	if m.CreatedAt == 0 {
		m.CreatedAt = now.UnixMilli()
	}
{{- else if eq $createdAtType "int32" }}
	if m.CreatedAt == 0 {
		m.CreatedAt = int32(now.Unix())
	}
{{- end }}
{{- end }}

{{- if $updatedAtType }}
{{- if eq $updatedAtType "time.Time" }}
	if m.UpdatedAt.IsZero() {
		m.UpdatedAt = now
	}
{{- else if eq $updatedAtType "*time.Time" }}
	if m.UpdatedAt == nil {
		m.UpdatedAt = &now
	}
{{- else if eq $updatedAtType "int64" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = now.UnixMilli()
	}
{{- else if eq $updatedAtType "int32" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = int32(now.Unix())
	}
{{- end }}
{{- end }}

	return nil
}

func (m *{{.Model}}) BeforeUpdate(ctx context.Context, query *bun.UpdateQuery) error {
{{- if $updatedAtType }}
{{- if eq $timezone "UTC" }}
	now := time.Now().UTC()
{{- else if eq $timezone "Local" }}
	now := time.Now().Local()
{{- else if $timezone }}
	loc, err := time.LoadLocation("{{$timezone}}")
	if err != nil {
		return err
	}
	now := time.Now().In(loc)
{{- else }}
	now := time.Now()
{{- end }}

{{- if eq $updatedAtType "time.Time" }}
	if m.UpdatedAt.IsZero() {
		m.UpdatedAt = now
	}
{{- else if eq $updatedAtType "*time.Time" }}
	if m.UpdatedAt == nil {
		m.UpdatedAt = &now
	}
{{- else if eq $updatedAtType "int64" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = now.UnixMilli()
	}
{{- else if eq $updatedAtType "int32" }}
	if m.UpdatedAt == 0 {
		m.UpdatedAt = int32(now.Unix())
	}
{{- end }}
{{- end }}

	return nil
}
